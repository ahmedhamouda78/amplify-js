---
phase: 01-foundation-and-guide-structure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - guide/00-introduction.md
  - guide/01-decision-framework.md
  - guide/02-parity-matrix.md
autonomous: true
requirements: [STRC-01, STRC-02, STRC-04]

must_haves:
  truths:
    - "Reader can follow the decision flowchart and arrive at a clear strategy recommendation for their use case"
    - "Reader can see exactly which DataStore features each strategy covers via the parity matrix"
    - "Reader sees before/after code snippets for core DataStore operations (save, query, delete) in the introduction"
  artifacts:
    - path: "guide/00-introduction.md"
      provides: "Understanding DataStore, what this guide covers, who it is for"
      contains: "## Understanding DataStore"
    - path: "guide/01-decision-framework.md"
      provides: "Decision flowchart, strategy descriptions, complexity warnings"
      contains: "## Decision Flowchart"
    - path: "guide/02-parity-matrix.md"
      provides: "Feature comparison table: 3 strategies vs DataStore"
      contains: "| Feature |"
  key_links:
    - from: "guide/00-introduction.md"
      to: "guide/01-decision-framework.md"
      via: "Navigation link at end of introduction"
      pattern: "\\[.*decision.*framework\\]"
    - from: "guide/01-decision-framework.md"
      to: "guide/02-parity-matrix.md"
      via: "Navigation link after flowchart"
      pattern: "\\[.*parity.*matrix\\]"
---

<objective>
Create the guide skeleton, decision framework, and feature parity matrix -- the structural foundation that helps readers choose their migration strategy before writing any code.

Purpose: Readers need to understand what DataStore does, decide which migration strategy fits their use case, and see exactly which features each strategy covers. This must happen before any technical setup content.

Output: Three guide files (introduction, decision framework, parity matrix) with navigation links between them.
</objective>

<execution_context>
@/home/ec2-user/.claude/get-shit-done/workflows/execute-plan.md
@/home/ec2-user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-guide-structure/01-RESEARCH.md
@.planning/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create guide directory and introduction page</name>
  <files>guide/00-introduction.md</files>
  <action>
Create `guide/` directory and write `guide/00-introduction.md` with these sections:

1. **Title and metadata comment** -- `# Migrating from DataStore to Apollo Client (JavaScript/TypeScript)` with an HTML comment block containing: guide version, last updated date, target audience (Amplify JS DataStore Gen 1 users migrating to Gen 2).

2. **Understanding DataStore** -- 2-3 paragraphs explaining what DataStore provides (local-first data layer, automatic sync, conflict resolution, offline support, real-time via observe/observeQuery). Frame it as: "DataStore gave you X, Y, Z automatically. This guide shows how to get equivalent capabilities with Apollo Client."

3. **What This Guide Covers** -- Bulleted list of the three strategies:
   - **API Only** (simplest): Direct GraphQL queries/mutations via Apollo Client. No local persistence. Best for apps that don't need offline or instant optimistic updates.
   - **Local Caching** (moderate): Apollo Client with persistent cache and optimistic updates. Near-offline experience without a full sync engine.
   - **Offline-First** (complex): Full offline architecture with Dexie.js local DB, mutation queue, sync engine, and conflict resolution. Closest to DataStore's full feature set.

4. **Who Should Use This Guide** -- Target audience: developers with existing Amplify Gen 1 apps using DataStore who are migrating to Gen 2. Assumes familiarity with React, GraphQL basics, and Amplify configuration.

5. **Quick Comparison: Before and After** -- A table showing 4-5 core DataStore operations with their Apollo equivalents at a glance (STRC-04 partial). Use this format:

```markdown
<!-- ai:comparison-table -->
| DataStore Operation | Apollo Client Equivalent |
|---------------------|--------------------------|
| `DataStore.save(new Post({...}))` | `apolloClient.mutate({ mutation: CREATE_POST, variables: {...} })` |
| `DataStore.query(Post)` | `apolloClient.query({ query: LIST_POSTS })` |
| `DataStore.query(Post, id)` | `apolloClient.query({ query: GET_POST, variables: { id } })` |
| `DataStore.delete(post)` | `apolloClient.mutate({ mutation: DELETE_POST, variables: { input: { id, _version } } })` |
| `DataStore.observe(Post)` | `amplifyClient.graphql({ query: onCreatePost }).subscribe(...)` |
```

6. **How to Use This Guide** -- Brief navigation: start with decision framework, check parity matrix, then follow setup and strategy-specific sections. Include links:
   - `[Choose Your Strategy](./01-decision-framework.md)`
   - `[Feature Parity Matrix](./02-parity-matrix.md)`
   - `[Apollo Client Setup](./04-apollo-setup.md)`

Use AI-friendly conventions throughout:
- Consistent heading hierarchy (H1 for page title, H2 for sections, H3 for subsections)
- HTML comment tags for machine-parseable sections: `<!-- ai:section-name -->`
- Tagged code blocks with language identifiers
  </action>
  <verify>
    <automated>test -f guide/00-introduction.md && grep -q "Understanding DataStore" guide/00-introduction.md && grep -q "decision-framework" guide/00-introduction.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>guide/00-introduction.md exists with all 6 sections, navigation links to other guide pages, AI-friendly structural conventions, and before/after comparison table</done>
</task>

<task type="auto">
  <name>Task 2: Create decision framework and parity matrix</name>
  <files>guide/01-decision-framework.md, guide/02-parity-matrix.md</files>
  <action>
**File 1: `guide/01-decision-framework.md`** (STRC-01)

Write the decision framework with these sections:

1. **Title** -- `# Choosing Your Migration Strategy`

2. **The Default Recommendation** -- Bold callout: "Start with API Only unless you have a specific need for caching or offline." Per locked decision, this is the strong default.

3. **Decision Flowchart** -- A text-based flowchart using this exact routing logic (from CONTEXT.md locked decision):

```markdown
<!-- ai:decision-tree -->
<!-- format: mermaid-compatible text flowchart -->
Start: "Does your app need to work offline?"
  ├── Yes → "Offline-First Strategy" (guide/10-offline-setup.md - Phase 4)
  └── No → "Does your app need instant UI updates before server confirmation?"
        ├── Yes → "Local Caching Strategy" (guide/07-cache-setup.md - Phase 3)
        └── No → "API Only Strategy" (guide/07-api-only-crud.md - Phase 2)
```

Render this as BOTH a Mermaid code block (for rendering tools) AND a plain-text version (for accessibility). The plain-text version should use indented arrows.

4. **Strategy Descriptions** -- One H2 section per strategy with:
   - **What it provides** -- 3-5 bullet points of capabilities
   - **What you give up** (compared to DataStore) -- honest list
   - **Complexity** -- effort estimate (e.g., "1-2 hours for basic setup" / "2-4 hours including cache persistence" / "1-2 weeks for full offline architecture")
   - **Best for** -- use case description
   - Match iOS/Android guides' complexity framing but do not exceed their warning level

5. **Important: Offline Might Not Be Required** -- A callout section explaining that DataStore gave offline "for free" but many apps never actually needed it. Help readers honestly assess whether they need offline support. Questions to ask: "Do your users actually use the app without connectivity? Do you have error reports about offline scenarios? Would a loading spinner during network issues be acceptable?"

6. **Navigation** -- Links to parity matrix (`./02-parity-matrix.md`) and back to introduction (`./00-introduction.md`)

**File 2: `guide/02-parity-matrix.md`** (STRC-02)

Write the feature parity matrix with these sections:

1. **Title** -- `# Feature Parity Matrix`

2. **How to Read This Matrix** -- Brief explanation: checkmark means the strategy supports the feature, with notes on any caveats.

3. **The Matrix** -- A markdown table with these columns:
   - Feature (DataStore capability)
   - API Only
   - Local Caching
   - Offline-First
   - Notes

Include these feature rows (minimum, derived from research and WIP guide):

| Feature | API Only | Local Caching | Offline-First | Notes |
|---------|----------|---------------|---------------|-------|
| Basic CRUD (save, query, delete) | Yes | Yes | Yes | |
| Filtered queries (predicates) | Yes | Yes | Yes | GraphQL filter syntax differs from DataStore predicates |
| Pagination & sorting | Yes | Yes | Yes | Cursor-based via nextToken |
| Relationships (hasMany, belongsTo, etc.) | Yes | Yes | Yes | Nested selections in GraphQL |
| Real-time (observe) | Yes | Yes | Yes | Via Amplify subscriptions, not Apollo |
| Real-time list (observeQuery) | Partial | Yes | Yes | API Only uses refetch; Local Caching uses watchQuery |
| Optimistic UI updates | No | Yes | Yes | Requires cache manipulation |
| Persistent cache (survives refresh) | No | Yes | Yes | apollo3-cache-persist / Dexie.js |
| Offline mutations (queue & replay) | No | No | Yes | Custom mutation queue required |
| Automatic sync (delta/base) | No | No | Yes | Custom sync engine required |
| Conflict resolution | No | No | Yes | Must handle _version manually |
| Selective sync (sync expressions) | No | No | Partial | Custom sync filters; not as declarative as DataStore |
| Hub events (lifecycle) | No | No | Partial | Custom event emitter needed |

4. **Legend** -- Explain Yes / No / Partial meanings.

5. **Key Takeaways** -- 3 bullet points summarizing the tradeoffs:
   - API Only covers ~70% of DataStore features with ~10% of the complexity
   - Local Caching adds optimistic updates and persistence for moderate additional effort
   - Offline-First approaches full DataStore parity but requires significant custom code

6. **Navigation** -- Links to prerequisites (`./03-prerequisites.md`) and back to decision framework (`./01-decision-framework.md`)

Use AI-friendly conventions:
- `<!-- ai:decision-tree -->` before flowchart
- `<!-- ai:parity-matrix -->` before the comparison table
- `<!-- ai:strategy:api-only -->`, `<!-- ai:strategy:local-caching -->`, `<!-- ai:strategy:offline-first -->` before each strategy section
  </action>
  <verify>
    <automated>test -f guide/01-decision-framework.md && test -f guide/02-parity-matrix.md && grep -q "Decision Flowchart" guide/01-decision-framework.md && grep -q "Feature Parity Matrix" guide/02-parity-matrix.md && grep -c "|" guide/02-parity-matrix.md | awk '{if ($1 >= 15) print "PASS"; else print "FAIL"}'</automated>
  </verify>
  <done>Decision framework has flowchart with two routing questions leading to three strategies, complexity warnings, and "offline might not be required" callout. Parity matrix has 13+ feature rows across 3 strategies with notes column. Both files have AI-friendly tags and navigation links.</done>
</task>

</tasks>

<verification>
1. All three guide files exist in `guide/` directory
2. Decision flowchart routes through exactly two questions to three strategies (per locked decision)
3. Parity matrix has at least 13 feature rows with all 3 strategy columns
4. Navigation links connect all three files in reading order
5. AI-friendly comment tags present (`<!-- ai:decision-tree -->`, `<!-- ai:parity-matrix -->`, etc.)
6. No mention of deferred items (owner-based auth, React Native, Next.js SSR, backend cleanup)
7. Complexity warnings match iOS/Android guide tone -- honest but not alarming
</verification>

<success_criteria>
- Reader can start at 00-introduction.md and understand what migration strategies exist
- Reader can follow the flowchart in 01-decision-framework.md and land on one of three strategies
- Reader can check the parity matrix in 02-parity-matrix.md to see exactly what each strategy covers
- All files use consistent heading hierarchy and AI-friendly structural tags
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-guide-structure/01-01-SUMMARY.md`
</output>
